The target platforms files are edited using the ".tdp" files.

The editor for these file is at:

https://github.com/mbarbero/fr.obeo.releng.targetplatform

The editor has to be installed in the IDE and can be used to convert
the "tdp" files to ".target" files used by Eclipse PDE and Tycho.